// Recursive source execution is represented by explicit runtime frames.
fn triangular(n) {
    if (n == 0) {
        return 0;
    } else {
        return n + triangular(n - 1);
    }
}

fn main() {
    return triangular(12);
}
